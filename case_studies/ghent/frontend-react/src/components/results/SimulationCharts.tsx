import React from 'react';
import { Text, Box } from '@mantine/core';

const SimulationCharts: React.FC = () => {
    return (
        <Box p="md">
            <Text size="sm" c="dimmed">Run a simulation to view time-series results.</Text>
        </Box>
    );
};

export default SimulationCharts;
